use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "replace-this-local-demo-key-before-sharing"
  limiter: false
  image_proxy: false

search:
  safe_search: 1
  autocomplete: ""
  default_lang: "auto"
  formats:
    - html
    - json

